<svg width="640" height="640" viewBox="0 0 640 640" xmlns="http://www.w3.org/2000/svg"><rect width="100%" height="100%" fill="white"/><g><rect data-type="rect" data-label="node top
z:0,3" data-x="0" data-y="0" x="180" y="180" width="280" height="280" fill="rgba(34, 197, 94, 0.28)" stroke="rgba(22, 163, 74, 0.95)" stroke-width="0.007142857142857143"/></g><g><rect data-type="rect" data-label="node bottom-support
z:2" data-x="0" data-y="0" x="40" y="40" width="560" height="560" fill="#d1fae5" stroke="#10b981" stroke-width="0.007142857142857143"/></g><g><rect data-type="rect" data-label="node bottom-support-outer-merge-0
z:3" data-x="-1.5" data-y="0" x="40" y="40" width="140" height="560" fill="rgba(59, 130, 246, 0.18)" stroke="rgba(37, 99, 235, 0.95)" stroke-width="0.007142857142857143"/></g><g><rect data-type="rect" data-label="node bottom-support-outer-merge-1
z:3" data-x="1.5" data-y="0" x="460" y="40" width="140" height="560" fill="rgba(59, 130, 246, 0.18)" stroke="rgba(37, 99, 235, 0.95)" stroke-width="0.007142857142857143"/></g><g><rect data-type="rect" data-label="node bottom-support-outer-merge-2
z:3" data-x="0" data-y="-1.5" x="180" y="460" width="280" height="140" fill="rgba(59, 130, 246, 0.18)" stroke="rgba(37, 99, 235, 0.95)" stroke-width="0.007142857142857143"/></g><g><rect data-type="rect" data-label="node bottom-support-outer-merge-3
z:3" data-x="0" data-y="1.5" x="180" y="40" width="280" height="140" fill="rgba(59, 130, 246, 0.18)" stroke="rgba(37, 99, 235, 0.95)" stroke-width="0.007142857142857143"/></g><g id="crosshair" style="display: none"><line id="crosshair-h" y1="0" y2="640" stroke="#666" stroke-width="0.5"/><line id="crosshair-v" x1="0" x2="640" stroke="#666" stroke-width="0.5"/><text id="coordinates" font-family="monospace" font-size="12" fill="#666"></text></g><script><![CDATA[
              document.currentScript.parentElement.addEventListener('mousemove', (e) => {
                const svg = e.currentTarget;
                const rect = svg.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                const crosshair = svg.getElementById('crosshair');
                const h = svg.getElementById('crosshair-h');
                const v = svg.getElementById('crosshair-v');
                const coords = svg.getElementById('coordinates');

                crosshair.style.display = 'block';
                h.setAttribute('x1', '0');
                h.setAttribute('x2', '640');
                h.setAttribute('y1', y);
                h.setAttribute('y2', y);
                v.setAttribute('x1', x);
                v.setAttribute('x2', x);
                v.setAttribute('y1', '0');
                v.setAttribute('y2', '640');

                // Calculate real coordinates using inverse transformation
                const matrix = {"a":140,"c":0,"e":320,"b":0,"d":-140,"f":320};
                // Manually invert and apply the affine transform
                // Since we only use translate and scale, we can directly compute:
                // x' = (x - tx) / sx
                // y' = (y - ty) / sy
                const sx = matrix.a;
                const sy = matrix.d;
                const tx = matrix.e;
                const ty = matrix.f;
                const realPoint = {
                  x: (x - tx) / sx,
                  y: (y - ty) / sy // Flip y back since we used negative scale
                }

                coords.textContent = `(${realPoint.x.toFixed(2)}, ${realPoint.y.toFixed(2)})`;
                coords.setAttribute('x', (x + 5).toString());
                coords.setAttribute('y', (y - 5).toString());
              });
              document.currentScript.parentElement.addEventListener('mouseleave', () => {
                document.currentScript.parentElement.getElementById('crosshair').style.display = 'none';
              });
            ]]></script></svg>