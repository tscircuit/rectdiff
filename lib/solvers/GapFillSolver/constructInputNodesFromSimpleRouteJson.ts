import type { CapacityMeshNode } from "lib/types/capacity-mesh-types"
import type { SimpleRouteJson } from "lib/types/srj-types"

/**
 * Construct input nodes using the Simple Route JSON obstacles
 */
export const constructInputNodesFromSimpleRouteJson = (
  srj: SimpleRouteJson,
): CapacityMeshNode[] => {
  const nodes: CapacityMeshNode[] = []

  // TODO

  return nodes
}
